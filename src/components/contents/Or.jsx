import { Typography } from '@material-ui/core';
import React from 'react';

export const Or = () => (
  <Typography variant="button" color="primary" style={{ marginTop: '1rem', marginBottom: '1rem' }}>
    or
  </Typography>
);

import { Typography } from '@material-ui/core';
import React from 'react';

export const Or = () => (
  <Typography variant="button" color="primary" style={{ marginTop: '2rem', marginBottom: '2rem' }}>
    or
  </Typography>
);
